---
description: Lore memory usage, session index and pending counts
allowed-tools: Bash
---

Run `python3 "${CLAUDE_PLUGIN_ROOT}/bin/lore.py" status` followed by `python3 "${CLAUDE_PLUGIN_ROOT}/bin/lore.py" doctor` and summarize. If either memory scope is past 80% usage, offer to consolidate.
