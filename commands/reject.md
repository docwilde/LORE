---
description: Reject staged lore proposals (ids or "all")
argument-hint: [id ...|all]
allowed-tools: Bash
---

Run `python3 "${CLAUDE_PLUGIN_ROOT}/bin/lore.py" reject $ARGUMENTS` and confirm what was rejected.
